"""Outbound calls to sibling services, one module per sibling called.

Menu calls exactly one today — the Restaurant Service — but the directory shape is kept
uniform with services that call more than one.

Docstring only: no re-exports.
"""
