-r ../common/requirements.txt

# httpx for the saga's outbound calls (payment, rider, and now order itself — D36);
# temporalio because this image runs both the API side and orchestrator-worker.
httpx==0.28.1
temporalio==1.31.0
