-r ../common/requirements.txt

# httpx for the request-path lookups and the saga hand-off into the Orchestrator Service.
# No temporalio (D36): this image no longer runs a worker, and holds no Temporal client of
# its own — orchestrator/requirements.txt is the only one that needs it now.
httpx==0.28.1
