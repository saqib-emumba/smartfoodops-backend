-r ../common/requirements.txt

# httpx for the request-path lookups and the saga's outbound calls; temporalio because
# this image also runs order-worker, which executes the saga.
httpx==0.28.1
temporalio==1.31.0
